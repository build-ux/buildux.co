import styled from 'styled-components';

import { fontFamilySans, weightMedium } from '../../theme/settings';

const TypeCopyLargeEmphasized = styled.span`
  font-family: ${fontFamilySans};
  font-weight: ${weightMedium};
  font-size: 2rem;
  line-height: 1.4;
`;

export default TypeCopyLargeEmphasized;
