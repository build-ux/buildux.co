import styled from 'styled-components';

import { fontFamilySans, weightRegular } from '../../theme/settings';

const TypeCopyLarge = styled.span`
  font-family: ${fontFamilySans};
  font-weight: ${weightRegular};
  font-size: 2rem;
  line-height: 1.4;
`;

export default TypeCopyLarge;
