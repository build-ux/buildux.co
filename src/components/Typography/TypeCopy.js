import styled from 'styled-components';

import { fontFamilySans, weightLight } from '../../theme/settings';

const TypeCopy = styled.span`
  font-family: ${fontFamilySans};
  font-weight: ${weightLight};
  font-size: 1.8rem;
  line-height: 1.333;
`;

export default TypeCopy;
