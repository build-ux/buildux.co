import styled from 'styled-components';

import { fontFamilySans, weightSemiBold } from '../../theme/settings';

const TypeHeading = styled.span`
  font-family: ${fontFamilySans};
  font-weight: ${weightSemiBold};
  font-size: 2.8rem;
  line-height: 1.143;
`;

export default TypeHeading;
